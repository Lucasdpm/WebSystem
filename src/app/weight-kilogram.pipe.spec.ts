import { WeightKilogramPipe } from './weight-kilogram.pipe';

describe('WeightKilogramPipe', () => {
  it('create an instance', () => {
    const pipe = new WeightKilogramPipe();
    expect(pipe).toBeTruthy();
  });
});
